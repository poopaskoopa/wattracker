<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug build only (this resource of the same name overrides the strict
    one in main/). Permits cleartext to the host's loopback as seen from the
    emulator: 10.0.2.2 is the emulator's alias for the host's loopback, so
    the cloud dev harness (http://10.0.2.2:8765) and a local server running on
    the machine under test can be reached during development. Everything else
    stays strict, and the release build never sees this file.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
